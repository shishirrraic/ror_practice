<!DOCTYPE html>
<html>
  <head>
    <title>PracticeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class = "works">
      <nav>
        <ul>
          <li><%= link_to "Home", "/"%></li>
          <li><%= link_to "Works", "/works"%></li>
	  <li><%= link_to "Skills", "/skills"%></li>
	  <li><%= link_to "Non Scaffold Skills", "/non_scaffold_skills"%></li>
        </ul>
      </nav>

    <%= turbo_frame_tag "modal"%>
    <%= yield %>
    </div>
  </body>
</html>
